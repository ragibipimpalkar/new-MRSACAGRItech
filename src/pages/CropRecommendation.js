import { Container, Typography } from "@mui/material";

function CropRecommendation() {
  return (
    <Container>
      <Typography variant="h4">Crop Recommendation</Typography>
      <Typography>Based on soil and climate.</Typography>
    </Container>
  );
}

export default CropRecommendation;
